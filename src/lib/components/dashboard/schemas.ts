/**
 * Re-export from $lib/schemas for backward compatibility.
 * Prefer importing from "$lib/schemas" or "$lib/schemas/dashboard".
 */
export {
	dashboardSchema,
	schema,
	type DashboardSchema,
	type Schema,
} from "$lib/schemas/dashboard.js";
